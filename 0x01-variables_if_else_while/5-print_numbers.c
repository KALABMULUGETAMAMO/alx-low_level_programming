/* 
 * auth: kalab mamo
 */

#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, except for q and e.
 *
 * Return: Always 0.
 */
int main(void)
{
	long number;

	for (number = '0'; number <= 10; number++)
			putchar(number);


	putchar('\n');

	return (0);
}
